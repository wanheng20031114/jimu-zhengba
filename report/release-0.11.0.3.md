# 积木争霸 0.11.0.3 正式发行记录

日期：2026-09-10。正式源码提交：`79d7961bb20e0691d7887dfa48a9ad2cf433e0b2`；公开标签：`v0.11.0.3`。

[GitHub 发行页](https://github.com/wanheng20031114/jimu-zhengba/releases/tag/v0.11.0.3) · [Windows 下载](https://github.com/wanheng20031114/jimu-zhengba/releases/download/v0.11.0.3/jimu-zhengba-0.11.0.3-Windows-x64.zip)

## 本版内容

在原 0.11.0.2 的个人击杀结算和联机修复上，加入 `322cfcf` 的拥挤交战修复：站稳攻击、坚守和工作的单位不再被后排持续推挤；恢复移动时恢复避让速度；自动交战允许改打已在射程内的敌人，明确攻击命令和前摇仍保持锁定。

从指定 Git 提交导出干净源码，未复制工作区未提交的导航原型及用户配置。CPU 蒙皮、共享路径和静态移动实验保持隔离，不以尚未证明收益的候选替换正式实现。

## 交付与校验

| 内容 | 值 |
|---|---|
| Windows 文件版本 | 0.11.0.3 |
| ZIP 大小 | 67,321,569 字节 |
| ZIP SHA-256 | `c26a0c97afa13b683ec727e438a9ee88375aca7085eb554fa5504c80a831fb3a` |
| PCK SHA-256 | `bd2da6d029f381da37571b5f3e14db51e8e80d38473cd6d309375d30f8e79cc7` |
| EXE SHA-256 | `214ebf12dd9f73bf628667f74d830f5ed206858da51597fcc0ecf9f526c26476` |
| GitHub 附件 ID | 554907842 |

匿名 GitHub API 已确认标签、公开状态、附件大小与摘要。默认 `builds/windows` 和版本目录 `builds/windows-0.11.0.3` 的 EXE／PCK 摘要一致；默认 ZIP 与版本 ZIP 一致。旧版本目录和历史发行附件保留。

Godot 首次导入对 `default_bus_layout.tres` 做了等值序列化归一化，添加资源 UID、展开默认标志并省略默认值；实际音量与效果参数保持提交中的数值。核心游戏源码、项目和导出配置与指定归档一致。

## Relay 兼容性

协议为 10，BUILD_ID 为 `0.11.0`，资源指纹为 `b78f1a5c73687b3ccdfe34f2f5a5a409f172235641322c9d02eb5376a3d14c7b`，均与线上一致；公共信任证书逐字一致。

线上 `jimu-zhengba-relay.service` 正常运行，部署负载 `d403f9736d5bf600`，8 房、每房最多 8 名真人，80 个传输连接名额。只读核查期间 PID 285128、重启次数 0、服务实例身份保持不变，近 30 分钟未发现脚本、引擎、TLS 或传输错误。展示用 RELEASE_ID 不参与兼容握手，因此本次无需部署或重启服务。

## 验证范围

- 交战补丁已在干净基线上通过 152 项拥挤交战、466 项攻击／追击时序和 124 项多人复制检查，详见 [交战修复说明](../docs/crowd-contact-combat.md)。
- **实际发行 EXE** 使用 Vulkan / Forward+ 在 RTX 3080 上启动 180 帧并正常退出，独立 stderr 为空。
- **相同正式 PCK + 官方 Godot 4.6.3** 通过 12 项窄范围主场景、版本、指纹、实验排除及 RVO 站稳／恢复检查。该项没有声称以发行 EXE 执行外部测试脚本。
- **相同正式 PCK + 官方 Godot 4.6.3，公网真实 Game**：两真人、两 Bot 在自建房完成 42 秒持续对局，47 项检查通过。房主从第 8 步推进至第 1241 步，观察窗口 41.065 秒，约 30 TPS；客户端收到 558 次快照，最大间隔 153 ms、零拒绝。10 条真人命令、可靠通知和最终结算均通过，无重连或中断。
- **实际发行 EXE 内置网络检查**：191 项通过，公网 DTLS 握手 1560 ms，协议／指纹匹配，自建房可靠结束。该入口验证真实发行模板的传输与资源，不将其称作完整 Game 长对局。
- 发行模板禁用路径覆盖；首两轮外部夹具参数未被接受，没有进入测试对局。其失败记录与成功的两段验证分开。正式 EXE／PCK 在验证前后摘要不变，未为运行夹具修改发行包。

验收后的第二次只读检查确认服务仍 active/running，PID、服务实例身份和重启次数均未变化；近 10 分钟脚本、引擎、TLS、传输及严重拒绝计数均为 0。日志中本轮 3 个连接、2 个临时房间均有对应退出事件。最后一条诊断日志结合紧随其后的 erase 操作，推断当时 rooms、sessions、connections 均归零；这不是直接读取实时房间字典。

所有验证房间按正常流程退出。已命令核实本轮 Godot 辅助进程全部退出，保留用户编辑器 PID 36624。

自动审批拒绝了对本轮临时目录 `.local/release-0.11.0.3-20260910` 的递归删除，返回原因仅为 `blocked by policy`。未绕过或重试该删除；目录暂留，统计为 367,015,565 字节。正式包位于 `builds`，验收摘要已保存至本报告和同名 JSON。其他实验工作区及回退快照继续保留。
