# 0.13.0 上海 relay 与客户端同步发布

2026-09-12，用户明确授权“更新到最新的，改动线上服务”。上海服务于北京时间 **16:54:55** 切换到新发布目录 `ffff03e29ba3922f`，状态 active/running，自动重启计数 0。原发布目录 `4b53ad5b01390aa8` 保留。独立的 `bot-jump-relay.service` 进程身份未变化。

## 交付与内容

- Windows 完整包：`builds/积木争霸-0.13.0-Windows-x64.zip`。
- 解包目录：`builds/windows-0.13.0`；EXE 文件/产品版本 `0.13.0.0`。
- 发行版本 `0.13.0`，网络协议 10，网络 Build `0.11.0`。
- 线上、实际客户端和当前工作区的内容清单均为 99 项资源，SHA-256：`6b78aff3c469e7f0d10b01271a44ab3fb399866c26d347978fbcad9485bf96eb`。
- 包含长矛兵、盾卫、战象、轻骑兵及既有数值调整，并包含用户当前保存的 1v1、3v3 地图修改。本任务没有继续制作工程兵等后续单位。

本次发布以 `1680529` 后的工作区为输入，在 `.local/relay-update-20260912/source` 固定快照后构建；用户原有编辑器、旧游戏及工作区修改保留。这里的“最新”包含已有未提交内容，因此不是仅从某个 Git 提交重建的包。完整本地文件哈希记录保存在 `.local/relay-update-20260912/snapshot-receipt.json`。

本任务 Git 提交只收录发布工具、自检、版本标识、说明和本报告。原有地图、长矛兵资源序列化、界面、音频及其他编辑器修改没有混入提交。当前工作区的 `data/content_manifest.json` 已生成到与线上一致，和对应的未提交资源一起保留为工作区修改；仓库中原有已提交清单不冒充本次工作区发布清单。

## 修复

1. Windows 构建入口在导出前生成内容清单，导出后检查源资源没有变化，并运行实际 EXE 的包内自检，通过后才生成 ZIP。
2. 使用原生进程等待导出结束，修正直接调用 GUI 版 Godot 导致 PowerShell 提前继续的问题；构建进程以隐藏方式启动。
3. 包内自检对六张 JSON 地图计算与生成器一致的哈希，能够拦截“地图已改但清单未更新”。
4. 清单生成器支持只读 `--check`，导入模块不再改写文件；relay 构建也拒绝过时清单。
5. 发布检查不再要求旧的恰好 75 项数值检查，改为当前阵容至少 90 项，并要求实际包指纹和地图校验数量匹配当前发布快照。
6. 同步发行号、Windows 文件版本及发行说明。

未关闭版本或指纹校验。此前单独打包的客户端会继续收到 `version_mismatch`，所有玩家需改用本次完整包。

## 验证

| 检查 | 结果 |
|---|---|
| 实际 Windows EXE 离线包检查 | 202 项通过，含 90 项资源数值检查、6 张地图哈希检查；退出码 0，标准错误为空 |
| 实际新 EXE → 上海 1v1 | 216 项通过，握手 546 ms；建房、配置席位、开始、结束及房间释放通过 |
| 实际新 EXE → 上海 4v4（6、7号席位留空） | 216 项通过，握手 560 ms；席位、阵营、空位及房间流程通过 |
| 修改地图但不更新清单 | 原生资源自检只报对应地图哈希失败；恢复文件后清单重新匹配 |
| 旧 EXE → 新上海 relay | 实测约 540 ms 返回 `version_mismatch`，证明兼容性校验仍生效 |
| relay 发布模板 | Linux/Windows 导出退出码 0、标准错误为空；Windows release 模板本地启动报告 editor=false、debug=false、dedicated_server=true |
| 线上实际 PCK | SFTP 取回只读验证，SHA-256 与已上传包一致；原生读取其清单，指纹与实际 Windows 包一致 |
| 部署与生命周期 | 13 项 readiness/回滚保护测试通过；清单变更、换行兼容、新增/删除资源测试通过 |

此次房间流程测试不加载完整战场，没有将它描述为多客户端长时间实战或性能测试。当前改动针对发布和内容一致性，既有单位阶段的战斗与模型验证见各单位报告。

## 发布指纹

| 文件 | SHA-256 |
|---|---|
| Windows ZIP | `081a52a624c7cbb9c2e95643148d9067a669e835da0d4582bdc2c4855095de35` |
| Windows EXE | `50898ba7118f8fdf464e2939d73aba956a25aeb2d04dabcfb249e86ce5253b1a` |
| Windows PCK | `b33648790b5c6f02b999c5039ad636ea1c276fff7fab01be236a35c8c3dcfa1b` |
| Linux relay EXE | `d9f79ab89b5ae369aeed11c6052d402e8218cd503bf85b4a235f9c30c46a7c63` |
| Linux relay PCK | `2dce69df815933094145edf0d316cbb08cf04231538a84db56edc05bf3e952a9` |

线上保留既有 Godot 4.7.2 release 模板、证书身份、UDP 24571、8 房间/每房 8 真人配置。Windows 游戏使用原有 Godot 4.6.3 导出。两端以实际应用协议和内容指纹校验，原生握手及房间流程均通过。

上传后、切换前再次确认线上 0 房间、0 会话、0 连接。启动失败时部署工具可恢复旧服务配置与发布链接；本次未触发回滚。

收尾已查询 Windows 进程：本阶段导入、导出、资源探针、房间探针及本地 relay 均已退出；保留用户正常编辑器及两个原有游戏进程。生产 relay 按用户要求持续运行。凭据、私钥、原始远端日志均未提交。
