# 0.12 中继正式包部署与回滚审查

审查对象为提交 **81962ed3c94d7f8dbbe580d73b519a511a70a518** 中的 `tools/deploy_relay.py`，文件 SHA-256 为 `6eb25a2bd61872b7c2e41969bc3032680118b39befa9e28e997a52a4c13fcc1c`。实际 Linux 发布包通过完整性检查；新增离线审查 **34/34** 通过，已有四项回滚检查 **4/4** 通过。这里验证的是包校验与部署控制流程，**不代表已经完成公网部署或持续对局验收**。

发布包仅交付 `jimu-relay` 与同目录的 `jimu-relay.pck`。Linux 可执行文件为未修改的 Godot 4.7.2 官方 release template，共 73,519,416 字节，SHA-256 为 `d9f79ab89b5ae369aeed11c6052d402e8218cd503bf85b4a235f9c30c46a7c63`；PCK 为 51,020 字节，SHA-256 为 `f4e86bc90b5b202593c5951c116a7b0a98bc7d077ec3f116f87cc6e57c77406d`。部署器核对官方模板固定摘要、包文件大小与摘要、receipt 中的当前源码摘要和路径边界。测试覆盖错误运行时、平台、模板归档、模板摘要、源码缺失或过时、越界源码、PCK 缺失或篡改以及多余文件，全部拒绝。

成功路径的 `ExecStart` 指向按内容摘要命名的发布目录中的可执行文件，匹配 PCK 紧邻该可执行文件；没有把不可变可执行文件与可变 `current` 下的 PCK 混配。上传先写暂存文件，验证摘要后再进入发布目录。不可变包上传失败时，不更换共享配置，也不停止现有服务。

审查发现并修复了旧事务边界：共享密钥、证书和配置原先在回滚保护范围外上传，中途失败可能留下不完整的磁盘配置。最终实现将共享配置首次写入及之后的切换、启动、就绪验证纳入回滚范围。内存 SSH/SFTP 故障注入分别模拟配置半写和启动身份无效，确认完整恢复旧配置、证书、密钥、systemd unit 与发布目录链接，再启动旧服务。测试没有连接真实 SSH，也没有访问真实部署凭据。

已有四项检查补充验证：先恢复旧发布与配置再重启；原先禁用自启的服务继续保持禁用；恢复配置失败时禁止启动半成品；停止服务失败时禁止改写仍在运行的配置。四项由协调任务对最终实现执行并报告通过，本次同时复核其测试定义；没有为归档重复运行。

配套 JSON 保存 34 项断言、四项回滚用例、测试定义摘要与准确包摘要。离线审查工具已退出并经命令核实，没有启动 Godot、服务器或网络辅助进程。公开报告不包含凭据、端点或临时私有目录。
