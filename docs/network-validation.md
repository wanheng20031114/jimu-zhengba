# 联网部署与验收

## 发布契约

本轮使用游戏版本 `0.6.0`、网络协议 `2`。42 项单位、建筑、科技和地图资源由 `tools/build_content_manifest.py` 生成内容清单，当前 SHA256 为：

```text
c40e7f08d4799f738314fb760e5d41dafa228ba2d6850d61ca1d6d153d7ef6b9
```

文本资源按 UTF-8、LF 规范化后计算指纹，二进制 `.res` 保持原字节；清单自身也固定 UTF-8、LF。这使 Windows 工作区、Git 的换行规范化和新克隆得到相同指纹。修改规则或地图后，先重建清单，再同时更新客户端和中继。客户端从清单原始字节计算 SHA256，服务端在进入房间前拒绝版本或指纹不一致的连接。

公开信任证书为 `scripts/network/relay_trust.crt`，公开连接设置为 `data/relay_endpoint.json`。部署私钥与管理凭据不进入 Git 或 Windows 发布包。原始证书、`data/*.json` 和 `scenes/maps/*_layout.json` 需显式纳入导出；原生资源允许由 Godot 转换为二进制并重映射。

## 中继边界

上海部署使用独立 `ashen-crown-relay.service`，监听 UDP 24571，默认同时容纳一个房间、最多四个真人。原有 UDP 24570 及其他服务保持原状；部署工具不会修改主机网络限速、云安全组或其他应用。服务端使用独立、校验过的 Godot 4.7.2 运行时，客户端保持 Godot 4.6.3。

中继管理邀请码、席位、队伍、Bot 配置和连接身份，不模拟战斗。房主执行 30 TPS 权威逻辑；指令归属由已认证连接绑定，序号重复的包不会进入 Game。每个收件人的私有数据和当前可见状态由房主筛选，客户端只接收自己的经济/队列、同盟及可见敌人。迷雾中的建筑只保留最后看到的静态记忆。

控制、可靠表现事件和不可靠快照使用独立 ENet 通道。房主到中继的快照进一步按收件人划分通道 2–5，避免不同玩家的序号互相淘汰；中继向每个客户端使用通道 2。快照发送保持每人 15 Hz，服务端以 20 Hz、容量 3 的令牌桶吸收网络聚簇。消息限制和身份校验始终生效；传输只解析有界 JSON 基础值，不解码 Object 或 Resource。

每局随机 `match_id` 隔离不同比赛，旧局在途事件、快照、命令和结算不会落入下一局。首次连接最多重试 20 秒；普通断线 10 秒后由 Bot 接管，120 秒内可恢复原席位。房主断线暂停对局，30 秒内恢复则继续，否则中止。可靠结算原子释放房间。

## 可重复验证

```text
python tests/network_runner.py local
python tests/network_runner.py game
python tests/network_runner.py expiry
python tests/network_runner.py commands
python tests/network_game_live_runner.py local
python tests/network_game_live_runner.py remote
python tests/network_game_live_runner.py impaired --seed 24572 --loss 0.05
python tests/network_release_runner.py "C:/Program Files/Godot/Godot.exe" --source
python tests/network_release_runner.py builds/windows/AshenCrown.exe
```

`local` 与 `impaired` 使用临时中继、独立测试证书和自己启动的进程。`remote` 使用已部署中继，测试结束主动释放房间。测试日志位于忽略的 `.local/network/`；runner 在正常、超时和异常退出时关闭辅助进程。部署的生产服务继续运行。

| 验证 | 已通过 | 内容 |
|---|---:|---|
| 原生 DTLS 协议 | 68/68 | 四人身份、旧协议拒绝、内容版本、限速、重复命令、跨局隔离、重连边界 |
| 游戏复制层 | 52/52 | 原生单位/建筑、隐私、迷雾、稳定 ID、动画插值、静默消失、280 单位消息预算 |
| 重连期限 | 19/19 | 实际 main 场景终止状态、解除暂停、结果界面及停止重试 |
| 命令参数边界 | 172/172 | 实际 2v2 场景拒绝容器、分数、非有限值、越界编号；所有拒绝均不改变金币、训练、集结点或单位命令 |
| 四进程公网实战 | 86/86 | 实际移动、生产、伤害、非法指令、Bot 接管、双方重连和四端可靠结算 |
| 5% 丢包/抖动实战 | 87/87 | 额外稳定观测段，完整四进程实战与网络统计 |
| 源码统一发布入口 | 57/57 | 42 项资源、公开证书、清单、真实部署握手及临时房间释放 |

期限测试显式移动本机单调时钟期限，不声称实际等待了 30/120 秒；生产常量另有边界检查。公网实战包含真实 10 秒 Bot 接管等待。

命令校验统一使用有限、整值且有界的整数检查，保留 JSON 将整数解码为 `float` 的正常行为。集结点在矿脉编号及目标类型确认后才修改；直接执行入口也拒绝无效归属与已结束对局，不能通过绕过提交队列触发经济操作。

## 扰动量化

注入器在测试进程内转发不透明 UDP 数据报，每个客户端及每次重连使用独立上游 socket，不解密 DTLS、不改服务器网络配置。客户端到中继的基础往返延迟为 150 毫秒、往返抖动范围 ±40 毫秒；另有 8% 包增加 90 毫秒延迟以制造乱序。丢包概率可设为 3%、4% 或 5%，从首次握手起生效。房主到另一客户端的消息需经过两段链路。

2026-09-09 使用种子 24572、5% 丢包概率及 15 秒稳定观测段的记录：

| 指标 | 结果 |
|---|---:|
| 数据报数 | 4,921 |
| 实际丢包 | 238，4.836% |
| 实际乱序递送 | 846 |
| 正常快照间隔样本 | 629 |
| 三个客户端间隔 P95 | 167 / 167 / 183 ms |
| 最大正常间隔 | 283 ms |
| 快照格式拒绝、时间倒退、计划外断线 | 均为 0 |

按收件人分离通道前，原受影响客户端的 P95 为 451 毫秒；分离后相同配置运行降至 167 毫秒。包顺序和线程调度会影响重复运行的精确数值。正常间隔统计排除主动断线与恢复阶段；这组数据是网络扰动验证，不是渲染 FPS 或公网实际延迟基准。

扰动握手期间有 4 条原生 DTLS `-30464` 拒绝诊断，已由有界重试恢复并单独计数；证书验证错误、其他原生错误以及所有脚本错误仍判失败。正常公网及源码发布入口检查的 stderr 为空。

## Windows 包自检

发布 EXE 的 `--network-smoke` 通过 Session 进入保存的 `release_probe.tscn`，不依赖 `tests/`、本机 `.local` 配置或私钥。它只验证资源与联网协议：创建临时 2v2 房间、设置 Bot 槽位、完成 start/finish 并收到可靠结算，不载入 Game、不生成单位、不增加金币。输出包含版本、清单指纹和结果，不包含地址、邀请码或 token。

实际 EXE 的最终验收应以 `network_release_runner.py` 输出 `passed: true`、`exported_template: true`、`stderr_empty: true` 为准；源码模式通过不能替代发布包验证。

部署命令与详细接口见 [中继文档](../server/README.md)。
