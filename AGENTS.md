# Agent Rules

1. **结束前清理进程**：任务结束前，必须关闭/停止所有因验证而启动的辅助进程（如测试、服务器、Godot无头进程等），除非用户要求保留。
2. **严格进程核实**：在声明清理完毕前，必须用命令核实。Godot项目需专门查找并停止带 `--headless`、`--check-only` 等参数的测试用 `Godot.exe`，**切勿误关用户的正常编辑器**。
3. **Godot节点编辑方式**：尽可能避免在脚本中动态生成 Godot 节点。需要新增或调整节点时，优先直接编辑 `.tscn` 场景、使用 Godot 编辑器/原生场景结构，保持接近人类在编辑器中搭建节点的方式。
4. **优先使用 Godot 原生能力**：实现功能时优先寻找并使用 Godot 原生提供的节点、资源、信号、方法和函数。遇到不确定的需求，应根据需求查找 Godot 官方文档中合适的解决方案，再决定实现方式。
5. **新节点需求先查文档**：遇到需要创建全新 Godot 节点、引入新节点类型或搭建新的场景结构时，应先考虑查询 Godot 官方文档，确认是否有更合适的原生节点、资源或推荐做法。
6. **禁止滥用兜底代码**：不要为了掩盖结构问题而堆叠宽泛的 fallback、空判断、动态探测或兼容分支。兜底逻辑只能在确有必要且行为明确时使用，并应保持简单、可解释。
7. **默认使用 UTF-8**：读取或编辑代码、场景、配置和文档时，默认使用 UTF-8 编码，尤其是包含中文内容的文件（如 `AGENTS.md`）。若出现乱码，应先用 UTF-8 重新读取确认内容，再进行修改。
8. **优先使用生图工具**：需要新的整套图像和动画素材时，优先调用 image 生图工具而不是利用代码生成图像；透明素材直接要求生图工具输出原生透明背景，后续代码不得再根据 RGB 背景推断 Alpha，透明度相关处理只做切帧、对齐、透明外边距裁切、网格分析、透明补边和无损打包等确定性操作。
9. **透明失败处理**：首次生图若不是真透明背景，可让生图工具对原图再做一次“背景提取”编辑。
10. **小任务轻量验证**：如果任务范围非常小且影响面清晰，不必执行过重、过宽的检查
